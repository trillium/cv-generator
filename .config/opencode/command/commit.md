---
description: Commit all changes with semantic message
---

Stage all changes in the repository. Run all tests and ensure they pass. If tests pass, commit the changes with a short, semantic message describing the purpose: "$ARGUMENTS". Do not push to remote automatically.

# Multi-Resume System Configuration

# Enable multi-resume features (server-side)
MULTI_RESUME_ENABLED=true

# Enable multi-resume features (client-side)
NEXT_PUBLIC_MULTI_RESUME_ENABLED=true

# Path to your PII data directory (required)
# This should point to the directory containing your data.yml file
# PII_PATH=/path/to/your/pii/directory

# Optional: Override default resume selection
# DEFAULT_RESUME_PATH=data.yml

# Optional: Custom location for resume index file
# RESUME_INDEX_FILE=resume-index.json

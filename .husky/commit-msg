# Auto-trim trailing whitespace from commit message
sed -i '' -e 's/[[:space:]]*$//' "$1"

# Auto-lowercase the first letter (after the type)
# This handles patterns like "feat: Fix bug" -> "feat: fix bug"
perl -i -pe 's/^([a-z]+(\([^)]*\))?:\s+)([A-Z])/\1\l\3/' "$1"

commitlint --edit $1

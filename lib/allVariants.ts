/**
 * allVariants: all unique resume/cover-letter types available in the app.
 * This is a static list to avoid importing Next.js/React components in the PDF script.
 */
export const allVariants = ["single-column", "two-column"];
